/* ============================================================
   云端配置 —— 已配置好（2026-08-22）
   ------------------------------------------------------------
   本文件已由房东本人提供密钥后预填完成：
   · BIN_ID       = jsonbin 仓库 ID（已创建，名 rental-manager）
   · MASTER_KEY   = jsonbin Master Key（已填入）
   部署到 GitHub Pages 后即可直接使用，无需再改本文件。
   （若想更稳妥，可在 jsonbin「API Keys」里新建一个只读/只更新的
     Access Key，把 ACCESS_KEY 填上并把 MASTER_KEY 清空。）
   ============================================================ */
window.APP_CONFIG = {
  /* jsonbin 仓库(Bin)的 ID */
  BIN_ID: '6a897afbf5f4af5e2934c783',

  /* 推荐：jsonbin 的 Access Key（Bins 读取/更新权限）——当前未用 */
  ACCESS_KEY: '',

  /* 备用：Master Key（权限最大，网页源码可见，请谨慎）——当前使用中 */
  MASTER_KEY: '$2a$10$lyXcMFIJ2dO3JT5cjjlJweyM3Tv3jlyq3ybA6JmcV9Q5V2dl87LQC'
};
